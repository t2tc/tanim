[backend]
domain = "tanim-test-server.vercel.app"
port = 26463
server-name = "tanim 测试服务器"
disable-html-interface = false

[user_management]
db_type = "sqlite"
db_file = "user.db"            # will create a new one if not available.
allow_register = true

[cache]
max_memory_usage = 1024        # MiB
